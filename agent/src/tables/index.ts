/**
 * Tables are auto-registered from src/tables/*.ts files.
 * Do NOT re-export tables here to avoid duplicate registration errors.
 *
 * Available tables:
 * - tripsTable: Stores user trips with map view configuration
 */

export default {};
